import { TilteCasePipe } from './tilte-case.pipe';

describe('TilteCasePipe', () => {
  it('create an instance', () => {
    const pipe = new TilteCasePipe();
    expect(pipe).toBeTruthy();
  });
});
