import { SafeSecurityPipePipe } from './safe-security-pipe.pipe';

describe('SafeSecurityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SafeSecurityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
